# md5sum

> Compute or check MD5 checksums

- Print a checksum:

`md5sum {{file}}`

- Print a checksum to a file:

`md5sum {{file}} > {{checksum}}`

- Check a checksum:

`md5sum --check {{checksum}}`
